import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ReportedPropertiesScreen = () => {
	return (
		<View>
			<Text>ReportedPropertiesScreen</Text>
		</View>
	);
};

export default ReportedPropertiesScreen;

const styles = StyleSheet.create({});
