import { StyleSheet, Text, View } from "react-native";
import React from "react";

const PropertyMapViewScreen = () => {
	return (
		<View>
			<Text>PropertyMapViewScreen</Text>
		</View>
	);
};

export default PropertyMapViewScreen;

const styles = StyleSheet.create({});
